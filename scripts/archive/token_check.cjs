const admin = require('firebase-admin'); const sa = require('./functions/service-account-key.json'); admin.initializeApp({ credential: admin.credential.cert(sa) }); const db = admin.firestore(); async function check() { const tokens = await db.collection('fcm_tokens').get(); let memId = null; tokens.forEach(t => { if(t.data().memberId) memId = t.data().memberId }); if(memId) { const mem = await db.collection('members').doc(memId).get(); console.log('Member:', mem.data().name, mem.data().pushEnabled); } else { console.log('No memberId found'); } process.exit() } check();
