---
description: 개발/배포 워크플로우 - 빌드, 테스트, 배포 명령어 자동 실행
---

// turbo-all

1. Run lint check with `npx eslint`
2. Run build with `npm run build`
3. Run deployment with `node scripts/deploy_all.cjs`
4. Run git add, commit, and push
