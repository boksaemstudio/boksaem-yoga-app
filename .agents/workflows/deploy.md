---
description: 린트 체크, 빌드, Git 커밋/푸시, Firebase 배포 자동화
---

// turbo-all

1. Run build: `npm run build`
2. Deploy all sites: `node scripts/deploy_all.cjs`
3. Git commit and push: `git add -A && git commit -m "deploy" && git push`
